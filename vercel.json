{
  "buildCommand": "pip install -r requirements.txt",
  "functions": {
    "api/*.py": {
      "runtime": "python3.9",
      "maxDuration": 10
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.py" }
  ]
}
